The code snippet has:

face-recognition.py

It detects faces from a given image and builds a mark on the faces.

face-recognition-video.py

It detects faces from a stream of frames in a video and marks on the faces.

face_recog.py

It detects whether a given image is of someone already in the trained data

test.py

It includes various code samples, involving numpy, matplotlib, keras, pandas, json and etc.